# Eventora

This project is split into two parts:

- `client/`: the frontend app
- `server/`: the Express API

Open [Eventora.code-workspace](/Users/gauravchaudhary/Desktop/Eventora/Eventora.code-workspace) in VS Code to load both folders cleanly.

The frontend source lives under `client/src/`.
The main backend entry point is `server/index.js`.
